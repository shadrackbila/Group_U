import 'package:flutter/material.dart';

class AdminDelete extends StatelessWidget {
  const AdminDelete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
