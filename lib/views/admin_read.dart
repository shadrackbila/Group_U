import 'package:flutter/material.dart';

class AdminRead extends StatelessWidget {
  const AdminRead({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
