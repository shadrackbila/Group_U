import 'package:flutter/material.dart';

class ApplicationDetailsDelete extends StatelessWidget {
  const ApplicationDetailsDelete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
